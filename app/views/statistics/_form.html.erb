<%if @statistic.user_id == nil%>
<%=form_with(model: @statistic, url: race_statistics_path(@statistic.race), method: 'post') do |f|%>
    <%=render 'form_partial', f:f%>
    <%end%>
<%else%>
<%=form_with(model: @statistic) do |f|%>
    <%=render 'form_partial', f:f%>
    <%end%>
<%end%> 