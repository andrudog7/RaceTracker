<div class="dashboard">
    
    <div id="race_types">
        <%dashboard.each do |type|%>
            <div id=<%=type%>><%=type%>
                <ul>
                <li>Total <%=type.pluralize%><br><%=user_races(type).count%></li>
                <li>Total Miles<br><%=current_user.total_race_type_distance(type)%></li>
                </ul>
                <%end%>
        </div>                           
    </div>
</div>

